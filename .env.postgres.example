### Local development Postgres environment example
# Copy to `.env.postgres` and fill real values. Do NOT commit `.env.postgres` with secrets.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=traveloptimizer
# The container will be configured to listen on this internal port (5433) and the compose mapping exposes host:5433
POSTGRES_PORT=5433

# Spring Boot datasource convenience variables (optional, Spring can read SPRING_DATASOURCE_* directly)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/traveloptimizer
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=change_me
